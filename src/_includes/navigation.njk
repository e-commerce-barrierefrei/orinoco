{# for item in collections.all | eleventyNavigation -%}
            {%- if item.url -%}
                {%- set isCurrent = (item.url == page.url) -%}
                {%- set isParent = item.url != '/' and item.url in page.url -%}
                <li class="nav__item{% if isCurrent or isParent %} nav__item--active{% endif %}">
                    <a class="nav__link" href="{% if isCurrent %}#main{% else %}{{ item.url | url }}{% endif %}" {% if isCurrent %} aria-current="page" {% endif %}>
                        {{ item.title }}
                    </a>
                </li>
            {%- endif -%}
        {%- endfor -#}

<nav aria-label="Haupt" class="aufklapp-bereich nav-main" id="nav-main">
	<button class="aufklapp-button" id="main-nav-toggle">{{ text }}</button>
	<ul class="aufklapp-bereich">
		<li><a href="#" class="aufklapp-button">Teesorten</a>
			<div class="aufklapp-bereich">Expando mit <a href="#">Tabstop</a></div>
		</li>
		<li><a href="#" class="aufklapp-button">Socken</a>
			<div class="aufklapp-bereich">Expando mit <a href="#">Tabstop</a></div>
		</li>
		<li><a href="#" class="aufklapp-button">Kopfhörer</a>
			<div class="aufklapp-bereich">Expando mit <a href="#">Tabstop</a></div>
		</li>
		<li><a href="#" class="aufklapp-button">Armreife</a>
			<div class="aufklapp-bereich">Expando mit <a href="#">Tabstop</a></div>
		</li>
		<li><a href="#" class="aufklapp-button">Smartphone-Hüllen</a>
			<div class="aufklapp-bereich">Expando mit <a href="#">Tabstop</a></div>
		</li>
	</ul>
</nav>

